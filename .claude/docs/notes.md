# Notes & SEO Prerender

Notes are markdown files with YAML-ish frontmatter (`title`, `date`, `description`, optional `unlisted: true`). Each gets a permalink at `/notes/<slug>` with fully server-rendered HTML (content, KaTeX math, plus per-page SEO meta).

## Vault Source (separate repo)

The Obsidian vault itself lives in the sibling repo `~/Documents/GitHub/notes`, **not** in this repo. `src/data/notes/` here is a disposable, gitignored copy regenerated by `scripts/sync-notes.js`, which runs automatically via the `predev`/`prebuild` npm hooks before `npm run dev` and `npm run build`. It copies everything from `../notes` except `.git`, `.obsidian`, `templates`, `.DS_Store`, and `.gitignore`.

**Do not edit files under `src/data/notes/` directly in this repo** — edits will be wiped the next time `npm run dev` or `npm run build` runs. Edit notes in `~/Documents/GitHub/notes` instead. If `../notes` doesn't exist next to this repo, the sync script fails loudly with a clear error rather than silently building stale/missing content.

This split happened because the vault had grown into a full personal note-taking system (with its own uncommitted drafts, Obsidian config, etc.) that didn't belong bundled with the site's own git history.

## CI: Cross-repo Deploy Trigger

`.github/workflows/deploy.yml` runs on **either** a push to this repo's `2026` branch **or** a `repository_dispatch` event of type `notes-updated`. The `notes` repo's `.github/workflows/notify-site.yml` sends that dispatch on every push to its `main` branch (via `gh api repos/paramt/paramt.github.io/dispatches`), so editing/committing a note triggers a full site rebuild+deploy even with no change here.

Because the workflow can be triggered by a `notes` push, it checks out **both** repos as explicit siblings under `github.workspace` (`path: paramt.github.io` and `path: notes`) so `scripts/sync-notes.js`'s `../notes` relative path resolves the same way in CI as it does locally. All subsequent build steps run with `working-directory: paramt.github.io`.

**Secret required:** `notes` repo needs a `SITE_DISPATCH_TOKEN` secret (a PAT with `repo`/Contents-write access to `paramt.github.io`) to call the dispatch API — triggering a dispatch requires write auth on the target even though both repos are public. No token is needed for the `notes` checkout itself since it's a public repo.

## Pipeline

Build order (`package.json`): `vite build` → `vite build --ssr src/entry-server.jsx --outDir dist-server` → `node prerender.js`. A single pass of `prerender.js` does **everything**: home, `/notes` listing, every `/notes/<slug>/index.html`, and `dist/sitemap.xml`. There is **no** separate `generate-notes-pages.js` — the SSR bundle is needed to render note bodies, so consolidating into prerender means `dist-server` is still available at that point (it's deleted at the end of prerender).

## Shared Loader (`src/data/notes-loader.js`)

Both `Notes.jsx` (client + SSR) and `entry-server.jsx` import from here. Uses `import.meta.glob('./notes/*.md', { query: '?raw', import: 'default', eager: true })` which resolves identically in client and SSR Vite builds. Exports `parseFrontmatter`, `getAllNotes()`, `getNote(slug)`.

## Note-local Images

Markdown note images live alongside the `.md` files in `src/data/notes/`. Local image references resolve to `/notes-media/<filename>`:

- Standard Markdown images: `![alt](image.png)`.
- Obsidian embeds are normalized: `![[image.png]]` and `![[image.png|alt text]]`.
- Bare Obsidian filenames are resolved by basename across the whole `src/data/notes/` tree, so `![[1.png]]` still works after moving attachments into a nested folder like `src/data/notes/images/...`.
- Relative paths are preserved when present, e.g. `![alt](images/post/foo.png)` or `![[images/post/foo.png]]`.
- External URLs and root-relative `/...` image URLs are left untouched.
- Dev server: `vite.config.js` serves `/notes-media/*` directly from `src/data/notes/`.
- Production build: `prerender.js` copies note-local image files (`png`, `jpg`, `jpeg`, `webp`, `gif`, `avif`, `svg`) into `dist/notes-media/`.

## SSR-safety in `Notes.jsx`

The component accepts `initialSlug` as a prop — it must **not** touch `window` at render time. Client entry (`src/notes-entry.jsx`) parses `window.location.pathname` once and passes it in; SSR passes the slug directly. The j/k keyboard-navigation effect (see below) reads/writes `window.location` inside `useEffect`, which is safe because effects don't run during SSR — there's no `popstate`/client-side routing beyond that; every navigation, including j/k, is a full page load. Both client entries (`main.jsx` and `notes-entry.jsx`) use `hydrateRoot` when server-rendered markup is present.

## Keyboard Navigation

On an individual note page, `j`/`k` navigate to the next/previous note (vim-style: `j` down/older, `k` up/newer), walking `listedNotes` — the same array and order the `/notes` listing renders, computed once at module load. Ignored with a modifier key held, or while focus is in an `<input>`/`<textarea>`/`contenteditable`. Deliberately scoped to **listed** notes only: the lookup is `listedNotes.findIndex(n => n.slug === note.slug)`, which returns `-1` (effect no-ops) when viewing an unlisted note, so it can't be used to sequence into unlisted content from a listed neighbor, and viewing an unlisted note directly gives no j/k sequence at all.

## Meta Injection

`notes.html` has a `<!--ssr-head-->` sentinel right before `</head>`. `prerender.js` replaces it per page with `<title>`, description, canonical, Open Graph, Twitter card, and (for note pages) an `Article` JSON-LD block.

## Gotchas

**`notes.html` must not contain its own `<title>`** — browsers use the first `<title>` they parse, so a baseline would override the injected per-page title.

**GitHub Pages URL-resolution:** Vite emits `dist/notes.html` (multi-page input), and prerender writes the processed listing to `dist/notes/index.html`. GitHub Pages resolves `/notes` to `notes.html` **before** `notes/index.html`, so the raw `<!--ssr-head-->` template would be served in prod with no title. `vite preview` uses the opposite order, so this bug does not reproduce locally. Prerender deletes `dist/notes.html` at the end as cleanup.

**`public/sitemap.xml` must not exist** — Vite would copy it over the generated one at `dist/sitemap.xml`.

**Root `.gitignore` had an `ideas.md` pattern** (for an unrelated root-level scratch file) that, without a leading slash, also matched `src/data/notes/ideas.md` and silently hid that vault note from `git status`. It surfaced during the notes-repo split (a plain directory diff caught it) — worth remembering that gitignore patterns without a leading `/` match at any depth, so a filename chosen for one purpose can shadow a same-named file elsewhere in the tree.

## Unlisted Notes

Unlisted notes behave like unlisted YouTube videos. The permalink works (the `index.html` is still generated), but:
- `<meta name="robots" content="noindex, follow" />` is injected, so crawlers that hit the URL via backlinks won't index it.
- The slug is excluded from `dist/sitemap.xml`.
- It's filtered from the `/notes` listing UI (`Notes.jsx`).

**Defaults to unlisted, not listed** (`notes-loader.js`: `unlisted: meta.unlisted !== 'false'`) — a note only stays listed if its frontmatter explicitly says `unlisted: false`. This matters because the vault sync copies whatever's on disk, including a blank/malformed file (no parseable frontmatter, e.g. an empty Obsidian-created draft) with zero validation. Before this default was flipped, such a file's `unlisted` silently evaluated to `false` (listed), and the listing page unconditionally did `n.date.split('-')` — a blank file has no `date` either, crashing the SSR build. The listing page's date usage (`isNew` calc and the `<time>` element) is now also null-guarded as defense in depth.

## Math Delimiters (Obsidian/Pandoc rules)

`remark-math` pairs any two single `$`s, so prose like `$100 raise into a pot of $100` became inline math. Obsidian (the authoring environment) follows Pandoc's stricter rules, so `remarkObsidianMath` in `Notes.jsx` runs **after** `remarkMath` and demotes any single-`$` `inlineMath` node back to literal text unless: the opening `$` is immediately followed by a non-space, the closing `$` is immediately preceded by a non-space, and the closing `$` is **not** immediately followed by a digit. It checks the raw source via `node.position` offsets (the node's `value` is unreliable — remark-math strips code-span-style padding). `$$` display math is left untouched. Note this is a parser-level fix: swapping KaTeX for MathJax would *not* change delimiter behavior, since `remark-math` decides delimiters before any renderer runs.

## Heading Anchors

`Notes.jsx` runs a custom `rehypeHeadingSlugs` plugin (before `rehypeKatex`, so heading text is read before KaTeX expands math into its verbose annotated markup) that assigns each `h1`/`h2` in note content a slug `id`, lowercased with non-alphanumeric runs collapsed to a single dash. IDs are unique document-wide; duplicate headings get `-1`, `-2`, ... suffixes (shared counter across h1 and h2, matching how `rehype-slug` works). Custom `h1`/`h2` renderers (`NoteH1`/`NoteH2`) prepend a `#` anchor (`.note-heading-anchor` in `index.css`) that's hidden until the heading is hovered/focused. `h3` and below are left untouched (no id, no anchor).

## Rich Rendering Mode (`rich: true`)

Opt-in per note via `rich: true` frontmatter (defaults off when omitted). Two features: a TOC sidebar (desktop-only, `≥ 1028px` — no mobile fallback by design) and footnote highlight popups (all widths: below 1028px the popup is centered at the bottom of the screen overlaying the note content; at `≥ 1028px` it moves into the sidebar column). Layout: sidebar (260px) + 32px gap + content column (720px) are centered **as one 1012px unit** (via `.notes-page--rich` margin override), so rich note content deliberately sits left of viewport center; on viewports too narrow to center the unit, `max()` calcs pin the sidebar to the left edge and push the content right instead of hiding the sidebar. The magic numbers (506/214/292/308) are derived in the comment above `.note-toc` in `index.css` — keep them in sync if any width changes.

**Table of contents:** a fixed-position `.note-toc` nav listing every `h1`/`h2`, with `h2`s indented (`.note-toc-sub`). Entries come from `extractHeadings()` in `Notes.jsx`, a markdown-level scan (ATX `#`/`##` lines, fenced code stripped) that **mirrors `rehypeHeadingSlugs`' slugify + dedup counter** so `href`s match the rendered heading ids. It also mirrors the `remarkEmDash` `--`/`->` substitutions for display text, and strips link syntax/emphasis markers. If you change the slug logic in one place, change both. Setext headings and headings inside blockquotes are not picked up (rare; deliberately unhandled).

**Footnote highlights + popup:** the `rehypeFootnoteHighlights` plugin (rich mode only, inserted before `rehypeKatex`) wraps each footnote ref plus what precedes it into a single `.note-fnhl` hover target, highlighted as a whole (amber background, padded, `box-decoration-break: clone` for line wraps) — the footnote number is inside the highlight. The wrapped portion is, in order: a trailing bracketed phrase (`(...)` — highlighted **without** the parens, which don't render at all), a trailing quoted phrase (`"..."` incl. quotes), the whole preceding inline element (`em`/`strong`/`code`/`a`), or the trailing word — hyphens/apostrophes are word-internal (`state-of-the-art` highlights whole), while leading punctuation is left outside the wrapper. A marker after a space wraps alone. Refs are collected first and spliced in reverse document order so indexes stay valid.

Hovering (or tapping, on touch — mobile browsers fire `mouseover` on tap) a `.note-fnhl` shows the footnote in `.note-footnote-popup`, a fixed box at the bottom of the TOC column on desktop and bottom-center of the screen on smaller viewports (it may overlay the TOC's tail or note content — it has a solid background). Content is cloned at hover time from the rendered footnote definition (`li#user-content-fn-N`, backref `↩` links stripped) into React state and injected via `dangerouslySetInnerHTML` — no duplicate markdown rendering. The popup **persists on unhover**; it's dismissed by any page scroll (window `scroll` listener) or replaced by hovering another footnote. All of this is in a client-only `useEffect`, so SSR just prerenders the wrapper spans.

## Tags (`/notes/tags`, `/notes/tags/<tag>`)

`getAllTags()` in `notes-loader.js` groups notes by tag, built only from **listed** notes, with the literal string `"unlisted"` filtered out of the tag set too — this is deliberate: `unlisted` is a real badge rendered next to tags (`note.unlisted && <span class="notes-tag notes-tag--unlisted">`) that must never become a browsable/linkable "tag," since that would make hidden content discoverable via tag navigation. `prerender.js` filters the same list again right at the tag-page generation loop, so it's unambiguous at that call site that no `dist/notes/tags/unlisted/` is ever written — combined with GitHub Pages 404ing any path with no matching file, `/notes/tags/unlisted` is unreachable in production. Client-side (dev server only, since there's no static-file 404 there), an unknown/excluded tag just falls through to the general `/notes` listing rather than crashing.

`/notes/tags` shows the full inline listing (a `<section id={tag}>` per tag with its complete note list, same as `/notes` itself does for all notes) — each section heading links to that tag's dedicated `/notes/tags/<tag>` page. This intentionally mirrors the `/notes` listing → individual note page relationship: a listing page showing title/date previews that link out to full pages is a standard, non-duplicate-content pattern, not the same thing as reproducing full note bodies. Each `/notes/tags/<tag>` page is fully statically generated by `prerender.js` (own `<title>`/canonical/OG/Twitter meta via `buildTagHead`, plus a `CollectionPage`/`ItemList` JSON-LD block) and included in `sitemap.xml` with `lastmod` taken from the tag's most-recently-dated note (`taggedNotes[0].date` — valid without a separate reduce because `notes-loader.js` already sorts all notes date-descending before `getAllTags()` groups them).

Both `"tags"` and any individual tag name are **reserved slugs** under `/notes/` — a real note file named `tags.md`, or named after an existing tag, would be shadowed by these routes. `Notes.jsx` resolves `/notes/<x>` in this order: real note slug, then `initialTag` (`/notes/tags/<tag>`) match, then the literal `"tags"` index, else the general listing.

Tag badges on individual note pages link to `/notes/tags/<tag>`. The `/notes` listing's inline per-note tag badges stay plain, non-clickable `<span>`s — they sit inside the `.notes-item` link wrapping the whole row, and nesting an `<a>` there would be invalid HTML.

## Sitemap

Generated at `dist/sitemap.xml` during prerender. Static routes (`/`, `/notes`, `/poker`, `/poker-analytics`) are hardcoded in `STATIC_SITEMAP_ROUTES` in `prerender.js`. Notes are appended from `getAllNotes()`, skipping `unlisted`.
